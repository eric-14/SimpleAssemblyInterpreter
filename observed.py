def get_pins(observed):
    keypad={
        "1":1, 
    }
    

    result  = []
    # coordinate (0,0 ) can be (0,x+1) can be (1, 0)
    for index, x in enumerate(observed):
        for index , k in  enumerate(x):
            if(k == ):
            result.append()
